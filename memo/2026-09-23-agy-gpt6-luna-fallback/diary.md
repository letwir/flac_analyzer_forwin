# Diary - agy GPT-6-Luna fallback - 2026-09-23

- Request: use GPT-6-Luna when agy cannot launch.
- Read the active BOOTSTRAP, agy, engineering, MANUAL, and router instructions before editing.
- Updated four authoritative policy/router files. The initial exact-text preflight rejected a stale expected skill line before writing; the retry used the actual current text.
- Strict lint first exposed a payload verb and an existing VCS wording collision. Rephrased them without changing the authorization rule.
- A newline-normalization command initially removed the adjacent coding-tip/fallback records due to a PowerShell string conversion error. Immediately restored both records, verified their contents, then reran lint and preflight successfully.
- No code implementation, deployment, stage, commit, or push was performed.
